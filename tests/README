DSMW tests :

    * install 3 mediawiki wiki1, wiki2, wiki3 with all extensions require by 
      DSMW extension (see the INSTALL file about the DSMW installation).

    * The admin user must be "wikiSysop" and the password "admin", for each mediawiki

    * Database name must be wikidbTest1 for wiki1, wikidbTest2 for wiki2 and wikidbTest3 for wiki3

    * Attention ˆ bien lancer les procŽdures de mise ˆ jours de la base lors de l'installation de SMW puis de DSMW  pour chacun des wiki!

    * rendre "image" accessible en Žcriture sur chacun des wiki

    * dans chacun des LocalSettings.php, mettre le Upload "true" et ajouter au LocalSettings.php :
$wgFileExtensions = array('jpg', 'pdf','xml');



    * Before executing the tests, you have to create a dump of the database :
        mysqldump -u "root" -p"root" wikidbTest1 > dump.sql
        mysqldump -u "root" -p"root" wikidbTest1 > dump1.sql
        mysqldump -u "root" -p"root" wikidbTest2 > dump2.sql
        mysqldump -u "root" -p"root" wikidbTest3 > dump3.sql

    * dump.sql must be in [wikiPath]/extensions/DSMW/tests

    * you have to replace the  following lines in the initWikiTest.sh file:
        mysql -u root -padmin < createDBTest.sql

        mysql -u root -padmin wikidbTest1 < dump1.sql
        mysql -u root -padmin wikidbTest2 < dump2.sql
        mysql -u root -padmin wikidbTest3 < dump3.sql

        by :

        mysql -u "myUser" -p"MyPassword" < createDBTest.sql

        mysql -u "myUser" -p"MyPassword" wikidbTest1 < dump1.sql
        mysql -u "myUser" -p"MyPassword" wikidbTest2 < dump2.sql
        mysql -u "myUser" -p"MyPassword" wikidbTest3 < dump3.sql

        "myUser" must have all privileges on the database server

    * Be sure that you have the executable permission on initWikiTest.sh

    * Put your wiki location in setting.php

    * Enlever le commentaire pour le lancement des tests dans AllTests.php